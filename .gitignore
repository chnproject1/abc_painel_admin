# Variáveis de ambiente — NUNCA subir
.env
.env.local
.env*.local

# Dependências
node_modules/

# Build
.next/
out/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Arquivos locais que não vão pro deploy
.claude/
*.xlsx
scripts/insert-lost-records-*.js

# Lotes gerados para a Spedy (o registro fica no OneDrive, não no git)
exports/

# Tabela de endereços por DDD — ~9 MB de dado derivado, regenerável com
# `npm run spedy:ceps`. Fica fora do git; o backup é o próprio OneDrive.
lib/ceps/
